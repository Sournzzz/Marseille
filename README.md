# Marseille

CLI file organizer built with Python.

## Features

- Organizes files by extension.
- Handles duplicate filenames.
- Preserves `.tar.gz`.
- Uses `pathlib`, `argparse` and `logging`.

## Run

```bash
python src/main.py organizer <directory>
